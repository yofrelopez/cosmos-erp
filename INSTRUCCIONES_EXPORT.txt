-- Script simple para copiar las tablas más importantes de local a Supabase
-- Ejecuta este comando en tu terminal para generar el SQL:

-- Opción 1: Si tienes PostgreSQL instalado con pg_dump
-- pg_dump -h localhost -U postgres -d erp_vd_cosmos --data-only --inserts -t "Company" -t "User" -t "Branch" -t "BankAccount" -t "Wallet" -t "Client" -t "PricingThickness" -t "MoldingTexture" -t "MoldingColor" -t "PricingMolding" -t "Quote" -t "QuoteItem" -t "PricingGlass" > datos-completos.sql

-- Opción 2: Conectarte con psql y copiar manualmente
-- psql -h localhost -U postgres -d erp_vd_cosmos
-- \copy (SELECT * FROM "Company") TO 'companies.csv' CSV HEADER;
-- \copy (SELECT * FROM "User") TO 'users.csv' CSV HEADER;

-- Opción 3: Usar DBeaver o pgAdmin para exportar como SQL

-- INSTRUCCIONES PARA EJECUTAR ESTE EXPORT:
-- 1. Instala PostgreSQL tools si no las tienes
-- 2. O usa una GUI como DBeaver (https://dbeaver.io/)
-- 3. Conéctate a: postgresql://postgres:admin123@localhost:5432/erp_vd_cosmos
-- 4. Exporta las tablas a SQL con formato INSERT
-- 5. Ejecuta ese SQL en Supabase SQL Editor
